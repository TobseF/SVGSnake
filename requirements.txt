# ---------------------------------------------------------------------------
# SVGO-UI — runtime dependencies
# ---------------------------------------------------------------------------

# The optimizer itself: a pure-Python 1:1 port of SVGO 4.1.0.
# No Node.js, no JavaScript interpreter needed.
svgo-py>=4.1.0

# UI toolkit (dark theme on top of Tkinter).
customtkinter>=5.2.0

# Image handling for the preview.
Pillow>=10.0.0

# Drag & drop of files from the OS file manager onto the window.
tkinterdnd2>=0.4.0

# SVG preview rendering.
# resvg is a browser-faithful renderer (handles gradients, clip-paths, CSS
# classes) and ships self-contained wheels, so no system libraries are needed.
resvg-py>=0.1.5

# Fallback renderer, used only if resvg is unavailable. Self-contained wheels.
pymupdf>=1.24.0
